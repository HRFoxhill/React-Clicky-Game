{
  "name": "reactpractice",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://hrfoxhill.github.io/React-Clicky-Game/",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "utils": "^0.3.1"
  },
  "scripts": {
    "start": "set PORT=3075 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
